
#ifndef SERVER_JOINER
#define SERVER_JOINER

#include <list>
#include "../common/Thread.h"
/*
class Joiner {
	public:
	explicit Joiner(std::list<command*>& l);
	void run();
	~Joiner();
	void end();	

	private:
	std::list<command*>& list;
	bool must_continue = true;	
};
*/
#endif
